import 'package:flutter/material.dart';

/// Brand palette — minimalist, high-contrast, water + sun.
class AppColors {
  const AppColors._();

  // Brand primaries
  static const Color deepWater = Color(0xFF0B3954);
  static const Color lake = Color(0xFF1E6091);
  static const Color sky = Color(0xFF4FA3D1);
  static const Color foam = Color(0xFFE6F3FA);

  // Accents
  static const Color sunrise = Color(0xFFFF8C42);
  static const Color catchGold = Color(0xFFF6BD60);
  static const Color reef = Color(0xFF06A77D);

  // Neutrals
  static const Color ink = Color(0xFF0E1116);
  static const Color slate = Color(0xFF4A5568);
  static const Color mist = Color(0xFFE2E8F0);
  static const Color paper = Color(0xFFF8FAFC);
  static const Color white = Color(0xFFFFFFFF);

  // Status
  static const Color success = Color(0xFF06A77D);
  static const Color warning = Color(0xFFF6BD60);
  static const Color error = Color(0xFFE63946);
}
